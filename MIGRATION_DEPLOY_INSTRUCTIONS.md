# 🚀 Инструкция по деплою миграции из старой системы

## 📋 Краткое описание

Выполнена миграция данных из дампа старой системы в новую архитектуру с поддержкой Content Projects. 

### ✅ Что было мигрировано:
- **117 пользователей** (включая админов и процессоров)
- **9 разделов документации** (привязанных к проекту "Umbra Platform Документация")
- **25 страниц документации** с полным контентом
- **Финансовые данные**: аккаунты, категории, проекты, транзакции
- **Новая архитектура**: документация теперь отображается как проекты

## 🔧 Изменения в схеме базы данных

### Новые модели:
```prisma
model content_projects {
  id                     String                    @id @default(uuid())
  name                   String
  description            String?
  type                   String                    @default("documentation")
  isActive               Boolean                   @default(true)
  createdAt              DateTime                  @default(now())
  updatedAt              DateTime                  @updatedAt
  documentationSections  documentation_sections[]
}
```

### Обновленные модели:
- `documentation_sections`: добавлено поле `projectId` для связи с проектами
- `UserRole`: добавлена роль `PROCESSOR`

## 🛠️ Шаги для деплоя на продакшен

### 1. Применение изменений схемы

```bash
# На продакшен сервере
npx prisma db push
```

### 2. Выполнение миграции данных

```bash
# Убедитесь, что дамп находится в правильной папке
node scripts/migrate-from-dump.js
```

### 3. Проверка результатов

```bash
# Запустите скрипт проверки
node scripts/verify-migration.js
```

### 4. Ожидаемые результаты проверки:
- ✅ Пользователи: 117 всего (5 админов, 8 процессоров)
- ✅ Проекты контента: 1 ("Umbra Platform Документация")
- ✅ Разделы документации: 9 (все привязаны к проекту)
- ✅ Страницы документации: 25 (22 опубликованы)
- ✅ Финансовые данные: аккаунты, категории, транзакции

## ⚠️ Важные моменты

1. **Backup**: Обязательно создайте резервную копию базы данных перед миграцией
2. **Downtime**: Миграция может занять несколько минут
3. **Rollback**: В случае проблем можно откатить изменения через резервную копию

## 🔍 Проверка после деплоя

1. **Интерфейс**: Проверьте, что документация отображается в разделе проектов
2. **Авторизация**: Убедитесь, что пользователи с ролью PROCESSOR могут войти
3. **Контент**: Проверьте несколько страниц документации на корректность отображения

## 📞 Поддержка

Если возникнут проблемы:
1. Проверьте логи приложения
2. Запустите `node scripts/verify-migration.js` для диагностики
3. В критических случаях восстановите из backup

## 🎯 Результат

После успешного деплоя:
- Вся документация из старой системы будет доступна в новом интерфейсе
- Пользователи смогут просматривать материалы через систему проектов
- Сохранится вся структура разделов и страниц
- Финансовые данные будут перенесены

**Статус**: ✅ Готово к деплою на продакшен
